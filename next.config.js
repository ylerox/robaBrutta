/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/robaBrutta",
  images: {
  unoptimized: true,
  }
};

module.exports = nextConfig
